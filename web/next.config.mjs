/** Static export: the console is a set of files, so the demo cannot go down. */
const config = {
  output: "export",
  trailingSlash: true,
  images: { unoptimized: true },
};

export default config;
